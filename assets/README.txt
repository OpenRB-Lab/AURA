Place the AURA architecture figure here as `aura_arch.png`.

The root README references it as: assets/aura_arch.png

Use the exact figure from the paper (the conversational Round-X diagram with
the MLLM thinker -> concept tokens -> Token/Audio projectors -> C2A/BiFAM ->
MusicGen decoder). Export it to PNG (or SVG and update the README <img src>).
An SVG stand-in of the overall architecture is available at
results/figures/overall_editor_style.svg if you want a temporary image.
